# Kubernetes Secret 模板
# 使用前请先创建实际的 Secret

# 方法 1: 使用 kubectl 直接创建（推荐）
# kubectl create secret generic openai-api-key \
#   --from-literal=OPENAI_API_KEY='your-api-key-here' \
#   -n deep-research

# 方法 2: 使用 base64 编码后的 YAML（不推荐，因为不安全）
# echo -n 'your-api-key-here' | base64
# 然后替换下面的 <base64-encoded-api-key>

apiVersion: v1
kind: Secret
metadata:
  name: openai-api-key
  namespace: deep-research
type: Opaque
data:
  # Base64 编码后的 API Key
  # 警告：不要将实际的 API Key 提交到版本控制系统！
  OPENAI_API_KEY: <base64-encoded-api-key>

